'use strict';

const ROLES = Object.freeze({ OWNER: 'Owner', ASSISTANT: 'Assistant', PLATFORM_ADMIN: 'PlatformAdmin' });
const USER_STATUS = Object.freeze({ ACTIVE: 'Active', DISABLED: 'Disabled' });

module.exports = { ROLES, USER_STATUS };
